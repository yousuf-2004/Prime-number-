#include<stdio.h>
int main(){
    int n,i,j,a;
    scanf("%d",&n);
    for(i=2;i<=n;i++){
        a=1;
        for(j=2;j<i;j++){
            
            if(i%j==0)
            {
                a=0;
                break;
            }
             
        }
        if(a==1){
                printf("%d ",i);
            } 
    }
    
    
}
